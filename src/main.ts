import { Plugin } from 'obsidian';
import { GitHubService } from './services/GitHubService';

export default class BlogPublisherPlugin extends Plugin {
  async onload() {
    console.log('Blog Publisher v2 placeholder');
  }
}
